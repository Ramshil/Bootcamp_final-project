commands to run
---------------

export FLASK_ENV=development
export FLASK_APP=my_website


flask initb # initializing the database (Need to do only once)
flask run # running the web application



*User can schedfule tasks for a coming week only.
